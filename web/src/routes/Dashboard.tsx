export function Dashboard() {
  return (
    <section>
      <h2>Dashboard</h2>
      <p>
        You are signed in. This shell is the foundation for the Phase 1.0 screens:
        daily-entry capture, stock by grade, and the sales flow. Each lands as its
        API slice ships.
      </p>
    </section>
  );
}
