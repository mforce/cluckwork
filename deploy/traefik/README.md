# Traefik dynamic config

Place local TLS certificate or middleware configuration here when deploying behind Traefik.

The scaffolded `docker-compose.yml` mounts this directory read-only at `/etc/traefik/dynamic`.
