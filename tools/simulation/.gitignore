# Generated by bootstrap.sh — runtime secrets and reset.sh output. Never
# commit real credentials or per-run artifacts (#243 Task 4).
.env.sim
.sim-cast.json
out/
*.pem

# Generated by run-baseline.sh (#243 Task 9) — one findings doc per run.
# The template itself (checked in) is the only tracked file in findings/.
findings/*.md
!findings/TEMPLATE.md
